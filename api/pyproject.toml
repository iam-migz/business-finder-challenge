[project]
name = "gsps-challenge-api"
version = "0.1.0"
description = "FastAPI service for the GSPS challenge"
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
]

[project.scripts]
gsps-api = "app.main:start"

[tool.uvicorn]
app = "app.main:app"
reload = true
